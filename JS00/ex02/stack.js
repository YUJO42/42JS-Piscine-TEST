// 스택으로 사용할 배열을 리턴
function stack_create() {
  let stack = [];
}

// 스택이 비어있는지 확인
// 비었으면 --> True
// 안 비었으면 --> false
function stack_empty() {
  //
}

// 스택 맨 위에 자료 추가
function stack_push(stack, data) {
  //
}

// 맨 위의 자료를 리턴
function stack_peek() {
  //
}

// 스택 맨 위의 자료를 지우는 함수
// Return --> 없음 XXX
function stack_pop() {
  //
}
